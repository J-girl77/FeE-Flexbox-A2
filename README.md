# FeE-Flexbox-A2
mall website project managed entirely with Git and GitHub. The focus is on using Git correctly (commits, messages, repository structure) while building a simple page that uses Flexbox and includes a basic form. 
